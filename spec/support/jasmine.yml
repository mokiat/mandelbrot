src_dir: 

src_files:
  - 'resources/jquery/*.js'
  - 'resources/oop-js/*.js'
  - 'resources/*.js'

stylesheets:

helpers:

spec_dir: spec

spec_files:
  - '**/*[sS]pec.js'

spec_helper: spec/support/jasmine_helper.rb

boot_dir: spec/support/boot

boot_files:
  - '**/*.js'
